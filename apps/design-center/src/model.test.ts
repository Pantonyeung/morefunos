import{describe,expect,it}from'vitest';import{createSeedPLineDocument}from'./model';describe('DesignDocument authority',()=>{it('creates one P-Line design document without business truth',()=>{const d=createSeedPLineDocument();expect(d.targetProfile).toBe('p-line');expect(d.schemaVersion).toBe('1.0');expect(d.runtimeManifest.productLineId).toBe('p-line');expect(JSON.stringify(d)).not.toMatch(/payment|printer|cashDrawer|firebase|orderTruth/i)});it('starts with a 1280x800 production frame',()=>{const d=createSeedPLineDocument();const root=d.nodes.find(n=>n.id==='root');expect(root?.width).toBe(1280);expect(root?.height).toBe(800)})});